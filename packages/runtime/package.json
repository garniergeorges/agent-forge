{
  "name": "@agent-forge/runtime",
  "version": "0.0.0",
  "description": "Runtime executed inside Docker containers (the agent process itself)",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "forge-runtime": "./dist/runtime.mjs"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outfile ./dist/runtime.mjs --target node --format esm",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-forge/core": "workspace:*",
    "@agent-forge/tools-core": "workspace:*",
    "@ai-sdk/openai": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ai": "^4.0.0",
    "yaml": "^2.6.0"
  }
}
